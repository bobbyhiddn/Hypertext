# Corrected Gemini face review — 2026-08-22 v2

The prior `operator-review-2026-08-22` acceptance is revoked; its defects are recorded in the sibling rejected matrix. This non-overwriting set reviews 14 regenerated canonical face templates and one fresh representative full-pipeline Card generated only after template convergence.

`contact-sheets/templates-v2.png` shows the shared family geometry and deliberate header-only Card subtype exceptions plus fixed-size Lot exceptions. `contact-sheets/validation-sample-v2.png` contains the accepted targeted-revision result. `image-validation-v2.json` verifies truthful PNG decoding and exact 1024×1536 dimensions; it is not a visual acceptance waiver.

The accepted sample is `002-anoint` (VERB/UNCOMMON). Its first candidate was rejected for a green one-diamond badge; the saved candidate is the single-change revision with exactly two white diamonds. Text, stats (4/4/3), type icon, art containment, language panels, and panel geometry were checked at full resolution.

Remaining limitation: Gemini still draws semantic icon captions such as `CLOSED BOOK` and `PENCIL` inside Lot template composition slots in addition to the canonical type labels. They are legible and slot counts/rewards are correct, but this is more verbose than the preferred icon-plus-type-label treatment and should not be treated as a future reference requirement.
